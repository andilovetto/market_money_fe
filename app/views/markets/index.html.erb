<!DOCTYPE html>
<html>
<head>
<style>
table {
  font-family: arial, sans-serif;
  border-collapse: collapse;
  width: 100%;
}

td, th {
  border: 1px solid #dddddd;
  text-align: left;
  padding: 8px;
}

tr:nth-child(even) {
  background-color: #dddddd;
}
</style>
</head>
<body>

<h2>Markets</h2>
<table>
  <tr>
    <th>Name</th>
    <th>City</th>
    <th>State</th>
    <th></th>
  </tr>
  <tr>
  <% @markets.each do |market| %>
    <div class="market-<%= market.id %>">
      <td><%= market.name %></td>
      <td><%= market.city%></td>
      <td><%= market.state %></td>
      <td> <%= button_to "More Info", market_path(market), method: :get, params: { market_id: market.id } %></td>
    </div>
  </tr>
  <% end %>
</table>
</body>
</html>
